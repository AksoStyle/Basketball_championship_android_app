pluginManagement {
    repositories {
        google()
        mavenCentral()  // Maven Central repository
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()  // Maven Central repository
    }
}
rootProject.name = "Basketball_Championship"
include ':app'
