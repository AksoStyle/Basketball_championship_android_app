<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.basketball_championship">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Basketball_Championship"
        tools:targetApi="31">
        <uses-library android:name="org.apache.http.legacy" android:required="false"/>

        <activity android:name=".reg_log.ProfileActivity"/>
        <activity android:name=".reg_log.LoginActivity" />
        <activity android:name=".reg_log.RegistrationActivity" />
        <activity android:name=".NewsActivity" />
        <activity android:name=".PlayersActivity" />
        <activity android:name=".ScheduleActivity" />
        <activity android:name=".ScoresActivity" />
        <activity android:name=".TeamsActivity" />
        <activity android:name=".AdministratorActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>